## Setup

Run `npm install`

Before running anything locally, create a `.env` file and initialize your env variable like so:

    `PORT=YOUR_PORT`

Start dev server

    npm run dev

Start prod server

    npm start
